appId: dev.carlo.lol-companion
productName: LoL Companion
directories:
  output: dist
  buildResources: build
files:
  - out/**
extraResources:
  - from: certs/riotgames.pem
    to: certs/riotgames.pem
# Icon: build/icon.png, generated by scripts/make-icon.mjs (electron-builder
# converts it per platform).
icon: build/icon.png
win:
  target: nsis
nsis:
  # Per-user one-click installer: no admin rights needed on friends' PCs.
  oneClick: true
  perMachine: false
  deleteAppDataOnUninstall: false
  artifactName: ${productName}-${version}-setup.${ext}

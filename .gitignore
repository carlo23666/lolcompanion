node_modules/
dist/
out/
# RAW live-game recordings (real riotIds — NEVER commit). Anonymize with
# `npm run fixtures:anonymize` and promote keepers to fixtures/liveclient/.
fixtures/recordings/
*.local
.claude/settings.local.json
.env
.DS_Store
coverage/
reports/
*.log
*.tsbuildinfo
